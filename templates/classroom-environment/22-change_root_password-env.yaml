resource_registry:
  # WARNING  You can have only ONE NodeUserData.
  # If you add another, the last will override the first
  # If you need more than one you will need to combine them.
  OS::TripleO::NodeUserData: /home/stack/templates/classroom-configuration/userdata_root_password.yaml

parameter_defaults:
  NodeRootPassword: redhat
